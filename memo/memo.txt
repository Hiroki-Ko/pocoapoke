■ClaudeFlare周りについて
◎ClaudeFlare Workers
　ClaudeFlareが用意してくれるサーバの代わり。
　自分でレンタルサーバー借りなくても、Cloudflare 上でプログラム（API）が動かせる。
　他ぼサーバ提供サービスとの違いとして、無料枠が大きく個人開発レベルならタダで動かせる
◎ClaudeFlare Pages
　静的サイト+APIをまとめて管理できるサービス
　※普通なら別々で管理して連携しないといけないところをまとめることができる(ドメインも同じ)
　特徴としてfunction配下に置いたコードをAPIとして動かすことができる(=中身はWorkersと同じエンジンで動く)
　ただしwindowsでのローカル開発では不安定
◎Vite
　ClaudeFlareとは無関係の「フロントエンド専用のローカル開発サーバー」(開発ツール)
　Pagesがローカル環境ではうまく動かせないので代わりに使う
◎Miniflare
　Workers をローカルで動かすためのエンジン
　Pagesのfunctionsでやりたいことを、ローカルではMiniflare(= Workers dev)で代用する

ローカル：
  フロント → Vite
  API → Workers dev（Miniflare）

本番：
  Pages にまとめてデプロイ


■フロント開発
C:\Users\User\my_apps\pocoapoke\frontend> npm run dev を実行
※ http://localhost:5173

■API開発
C:\Users\User\my_apps\pocoapoke> wrangler pages dev frontend を実行
C:\Users\User\my_apps\pocoapoke> npx wrangler pages dev ./frontend を実行
※ http://127.0.0.1:8788
・起動コマンド
  wrangler dev -> Workersモード(Workers dev(Miniflare)) 今回の開発では使わない
  wrangler pages dev -> Pagesモードのベース
  ★ npx wrangler pages dev dist
    wranglerをAPI専用として起動->npm run dev側でデータの取得をしつつHMRを動かせる
    ※distを付けたことでdistを静的ファイルとして配信することを宣言

両方を起動した状態であればフロント⇔APIの疎通確認が可能

wrangler d1 execute pocoapoke --local --file=./seeds/pokemon_ms.sql
wrangler d1 execute pocoapoke --local --file=./seeds/master_code.sql

Workers用D1実行コマンド
npx wrangler d1 execute pocoapoke_db --file=schema.sql

D1からのexport文
・ローカルD1(Miniflare)
npx wrangler d1 execute pocoapoke --command "SELECT * FROM master_code" --json > public/masterCodes.json
・本番D1
npx wrangler d1 execute pocoapoke --remote --command "SELECT * FROM master_code" --json > public/masterCodes.json
・UTF-8からBOMを削除
Get-Content public/masterCodes.json | Set-Content -Encoding utf8 public/masterCodes_fixed.json
Move-Item -Force public/masterCodes_fixed.json public/masterCodes.json

npx wrangler d1 execute pocoapoke --command "SELECT class, json_group_array(json_object('id', id, 'code', code, 'label', label)) AS items FROM master_code GROUP BY class;" --json > public/masterCodes.json

■本番D1をローカルD1に適用
1. 本番からdump
  npx wrangler d1 export pocoapoke --remote --output=prod_dump.sql
2. ローカルのデータを削除
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS pokemon_status"
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS pokemon_ms"
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS master_code"
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS d1_migrations"
  wrangler d1 execute pocoapoke --local --command "DELETE FROM sqlite_sequence"
3. ローカルに本番のデータを入れる
  wrangler d1 execute pocoapoke --local --file=./prod_dump.sql


  wrangler d1 execute pocoapoke --remote --command "DROP TABLE IF EXISTS pokemon_status"
  wrangler d1 execute pocoapoke --remote --command "DROP TABLE IF EXISTS pokemon_ms"
  wrangler d1 execute pocoapoke --remote --command "DROP TABLE IF EXISTS master_code"
  wrangler d1 execute pocoapoke --remote --command "DROP TABLE IF EXISTS d1_migrations"
  wrangler d1 execute pocoapoke --remote --command "DELETE FROM sqlite_sequence"
  wrangler d1 execute pocoapoke --remote --file=./seeds/schema.sql
  wrangler d1 execute pocoapoke --remote --file=./seeds/master_code.sql
  node seeds/seed_pokemon_ms.cjs --remote
  wrangler d1 execute pocoapoke --remote --file=./seeds/pokemon_status.sql
■初期データ登録
1. ローカルのデータを削除
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS pokemon_status"
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS pokemon_ms"
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS master_code"
  wrangler d1 execute pocoapoke --local --command "DROP TABLE IF EXISTS d1_migrations"
  wrangler d1 execute pocoapoke --local --command "DELETE FROM sqlite_sequence"
2. schema.sql を実行。テーブル構成を反映。
  wrangler d1 execute pocoapoke --local --file=./seeds/schema.sql
3. MasterCodesを登録
  wrangler d1 execute pocoapoke --local --file=./seeds/master_code.sql
4. Pokemon_msを登録
  # ローカルD1
  node seeds/seed_pokemon_ms.cjs
  # リモートD1
  node seeds/seed_pokemon_ms.cjs --remote
  構成）
  pokemon_ms.json を直接参照
  master_code.sql からラベル→codeのマッピングを自動構築
  1件ずつ小さなSQLファイルを生成して wrangler d1 execute --file で実行（SQLITE_TOOBIG 回避）
5. Pokemon_Statusを登録
  wrangler d1 execute pocoapoke --local --file=./seeds/pokemon_status.sql


■D1へテーブル追加
1. schema.sql にテーブルの構造を追加
2. C:\Users\User\my_apps\pocoapoke> で↓を実行(ローカルD1に適用)
   wrangler d1 execute pocoapoke --local --file=./schema.sql
3. C:\Users\User\my_apps\pocoapoke> で↓を実行(本番D1に適用)
   wrangler d1 execute pocoapoke --file=./schema.sql
   npx wrangler d1 execute pocoapoke_db --file=./schema.sql --remote


■D1からテーブル削除
1. drop.sql を作成
   DROP TABLE IF EXISTS test_name;
2. C:\Users\User\my_apps\pocoapoke> で↓を実行しローカル、本番に適用
   wrangler d1 execute pocoapoke --local --file=./drop.sql
   wrangler d1 execute pocoapoke --file=./drop.sql
・テーブルの状況確認
npx wrangler d1 execute pocoapoke --remote --command "SELECT name FROM sqlite_master WHERE type='table';"
・指定のテーブルを削除
npx wrangler d1 execute pocoapoke --remote --command "DROP TABLE IF EXISTS pokemon_ms;DROP TABLE IF EXISTS master_code;DROP TABLE IF EXISTS pokemon_status;"


■カラム追加
1. alter.sql を作成
   ALTER TABLE pokemon_ms ADD COLUMN description TEXT;
2. C:\Users\User\my_apps\pocoapoke> で↓を実行しローカル、本番に適用
   wrangler d1 execute pocoapoke --local --file=./alter.sql
   wrangler d1 execute pocoapoke --file=./alter.sql

// INSERT文ひな形
INSERT INTO master_code (class, value, created_at)
VALUES
  ('specialty', 'fire', datetime('now')),
  ('specialty', 'water', datetime('now')),
  ('specialty', 'grass', datetime('now')),
  ('status', 'active', datetime('now')),
  ('status', 'sleep', datetime('now')),
  ('place', 'home', datetime('now')),
  ('place', 'field', datetime('now'));

INSERT INTO pokemon_ms (name, specialty, created_at)
VALUES
  ('Pikachu', 1, datetime('now')),  -- specialty = fire
  ('Squirtle', 2, datetime('now')); -- specialty = water

INSERT INTO pokemon_status (poke_id, status_code, place_code, created_at)
VALUES
  (1, 4, 6, datetime('now')),  -- Pikachu, active, home
  (2, 5, 7, datetime('now'));  -- Squirtle, sleep, field




// wrangler.toml
name = "pocoapoke"
main = "functions/index.ts"
compatibility_date = "2024-03-01"

# ローカル用
[[d1_databases]]
binding = "DB"
database_name = "pocoapoke_db"
database_id = "pocoapoke_db"

# 本番用
[env.production]
[[env.production.d1_databases]]
binding = "DB"
database_name = "pocoapoke_db"
database_id = "8ab074f8-09c5-4b3e-a446-d9d16c21a5df"


【プロジェクト概要】
- Cloudflare でゲーム進捗管理アプリを作成中
- フロント：React + Vite
- バックエンド：Cloudflare Functions（Workers）
- DB：D1

【問題】
- Windows で Pages dev が壊れており、静的ファイルと Functions が正しく動かない
- そのためローカルで API が動かず、フロントから fetch が失敗していた

【理解したこと】
- Workers = Cloudflare のサーバー
- Pages = 静的サイト + API をまとめて公開できるサービス
- Pages Functions の中身は Workers と同じ
- Vite = フロントをローカルで動かすツール
- Miniflare = Workers をローカルで動かすエンジン
- wrangler dev = Miniflare を使って Workers をローカルで動かすコマンド

【ローカル開発の最適解】
- フロント：Vite（5173）
- API：Workers dev（Miniflare）（8787）
- Pages dev は使わない（Windows で壊れるため）

【次にやるべきこと】
1. プロジェクトルートに wrangler.toml を置く
2. Workers 用に設定する
3. `npx wrangler dev` で API を起動
4. `npm run dev` でフロントを起動
5. フロントの API URL を 8787 に向ける

==========================================
React Query(ライブラリ)
プロパティ	 説明
data	      取得したデータ（成功時）
isLoading	  初回ロード中
isFetching	再フェッチ中（初回も含む）
error	      エラー内容
refetch	    手動で再フェッチする関数